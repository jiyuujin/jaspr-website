import 'package:jaspr_website/constants/theme.dart';

abstract class DesignTokens {
  static final primary = primaryColor;
  static final accent = accentColor;
  static final background = backgroundColor;
  static final text = textColor;
  static final muted = mutedColor;

  static const sectionPadding = 80;
  static const containerWidth = 1000;
}
